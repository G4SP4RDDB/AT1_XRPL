# Friction log — raw findings (append-only, both people)

Header for the report: **Track 1, open-ended** · flavour: Loaded (native 2-of-2 multisig composed as the call-date enforcer on the final `LoanPay`) · environment: custom hackathon devnet `lending-hackathon.dev.ripplex.io` (rippled 3.4.0-rc1, reserve 10 XRP base + 2 XRP per object) · library: `xrpl.js 5.2.0`.

| # | Category | Title | Description | Repro | Severity | Library + version | Proposed fix |
|---|---|---|---|---|---|---|---|
| 1 | docs / wrong model | `LoanDraw` does not exist | Hackathon material and our own docs assumed a separate drawdown transaction. Principal moves when `LoanSet` validates. | `node_modules/xrpl/dist/npm/models/transactions/` has no loanDraw | medium | xrpl.js 5.2.0 | XLS-66 overview: state that `LoanSet` originates and disburses in one transaction; add a lifecycle diagram listing every transaction type by stage |
| 2 | protocol / wrong model | Open-ended vault liquidity is not a lock | Coupons and later deposits refill `AssetsAvailable`; withdrawals are first come first served, so a depositor can pull principal out of coupon liquidity. | to reproduce in S2 | high | xrpl.js 5.2.0 | `AssetsMaximum` at creation as workaround; XLS-65 docs should describe how `AssetsAvailable` evolves through the loan lifecycle; propose a principal-locked shares flag |
| 3 | protocol / workaround | No earliest-close term on `LoanSet` | The protocol prices an early close (`CloseInterestRate`, `ClosePaymentFee`) but cannot forbid it before a date, so a bond-like product needs a trusted co-signer. | S1 Q2 | high | xrpl.js 5.2.0 | Optional earliest-close ledger time on `LoanSet`, enforced by the ledger |
| 4 | infra | Venue network drops TLS on ports 51233 / 51234 | TCP connects but the TLS handshake never completes on any rippled port, including `s1.ripple.com:51234`; port 443 works. The faucet (443) is fine. | `curl -v https://lending-hackathon.dev.ripplex.io:51234` stalls after Client Hello | high (blocks all on-chain work on venue wifi) | n/a | Expose the hackathon devnet WSS and JSON-RPC on 443 as well; tell participants up front |
| 5 | sdk | `Client` option is `connectionTimeout`, not `timeout` | `new Client(url, { timeout })` compiles (it is the request timeout) but does not extend the 5 s connect timeout; the error text names the right option only after the fact. | `src/chain/client.ts` first version | low | xrpl.js 5.2.0 | Name the two timeouts distinctly in the `Client` docs, or default connect timeout higher |
| 6 | docs / broken | Workshop slide tutorial link 404s | The intro deck's "Step-by-Step Lending Walkthrough" points to `xrpl.org/docs/tutorials/lending`, which returns 404. The XLS-65/66 specs on GitHub and the reference app README are the working sources. | `curl -I https://xrpl.org/docs/tutorials/lending` | low | n/a | Fix the slide link or publish the tutorial at that path |
| 7 | docs / spec vs implementation | Spec says `LoanSet` books `InterestDue` into `AssetsTotal`; the devnet does not | XLS-66 §3.8.6 item 6 reads "Increase `Vault.AssetsTotal` by `InterestDue`". On rippled 3.4.0-rc1, `AssetsTotal` stayed 1000000000 after `LoanSet` (TotalValueOutstanding 1000011416, so InterestDue was 11416 drops) and rose by 5652 drops only after the first `LoanPay` (interest 5709 minus 1 % fee). We first planned the whole dashboard around the spec text. | LoanSet 9049D4BC738E then vault_info; LoanPay 4434E33720D1 | high | xrpl.js 5.2.0, rippled 3.4.0-rc1 | Correct §3.8.6 or mark the behaviour by amendment; add `InterestDue` to the `Loan` entry so clients can show booked vs realised yield without recomputing amortisation |
| 8 | protocol / expectation | `AssetsMaximum` blocks origination | A cap equal to the raise makes `LoanSet` fail with `tecLIMIT_EXCEEDED` because origination adds `InterestDue` to `AssetsTotal` and the cap must exceed `AssetsTotal + InterestDue` (§3.8.5.2 items 6, 14). The result code does not say which of the two cap checks failed. | to reproduce in S2 | medium | xrpl.js 5.2.0 | Document that the cap must include expected interest; distinct result code or `vault_info` field showing the required headroom |
| 9 | docs / gap | Annualised rates make short test loans earn almost nothing | Rates are 1/10 bp annualised with a 60 s minimum interval. A 9-minute 1000 XRP loan at the 100 % maximum earns ~0.017 XRP, and small principals hit `tecPRECISION_LOSS` (§3.8.5.2 items 15-18). Nothing in the docs warns that devnet demos need large principals or long intervals. | config maths, to confirm in S1 | medium | xrpl.js 5.2.0 | A "testing on devnet" note with worked numbers; an SDK helper that computes `PeriodicPayment` and `InterestDue` before submitting |
| 10 | protocol / error message | `VaultDeposit` of the whole balance fails as `tecINSUFFICIENT_FUNDS` | Lender held exactly 1000 XRP and deposited 1000. The devnet reserve is 10 XRP base + 2 per object, and the deposit also creates the share `MPToken` object, so the real cause is reserve, not funds. The code does not say so. | tx 90E991C72D74 (spike run 1) | low | xrpl.js 5.2.0 | Return `tecINSUFFICIENT_RESERVE` when the shortfall is the reserve; SDK pre-check that subtracts reserve + new-object reserve before submitting |
| 12 | protocol / expectation | `LoanManage` impair refused with `tecTOO_SOON` on a current loan | With `fixCleanup3_4_0` active, `tfLoanImpair` is only accepted once `currentTime > NextPaymentDueDate`. The main spec text lists it only as an amendment bullet in §3.10.4.2 item 9, and the workshop material presents impairment as a discretionary broker action. `tfLoanUnimpair` on a non-impaired loan then returns `tecNO_PERMISSION`, which reads as an auth problem. | LoanManage E69143C1822C, 3CA1EDC1FD58 | medium | xrpl.js 5.2.0, rippled 3.4.0-rc1 | State the overdue precondition in the LoanManage overview; distinct code (`tecNO_ENTRY`-style "not impaired") for unimpair on a healthy loan |
| 13 | docs / gap | Where each loan fee goes is not summarised | The early close sent the 1 % `CloseInterestRate` penalty to the vault (PPS 1.0000056 to 1.0066) and the 1 XRP `ClosePaymentFee` to the broker; `ManagementFeeRate` skims the interest of each coupon (57 of 5709 drops). This is scattered across §3.11.5 and Appendix A-3, no single table. It matters because `CloseInterestRate` turns out to be a ledger-enforced economic yield lock for bond-like vaults. | LoanPay full 685A52185C45: offered 681001942, charged 674.335291 XRP | medium | xrpl.js 5.2.0 | One "fee destinations" table in XLS-66: fee, who pays, who receives, when |
| 11 | sdk / expectation | `signLoanSetByCounterparty` `multisign` string means the signer's X-address, not the multisig account | Passing the borrower account address as `multisign` (the natural reading: "sign on behalf of this multisig account") set `Signer.Account` to the borrower for both signers, so the SDK rejected the combined transaction with "Duplicate Signers not allowed". The boolean form is the correct one; a string is used verbatim as `Signer.Account` (the signer's own address, X-address accepted), the same convention as `Wallet.sign` (verified in `Wallet/counterpartySigner.js`). | spike run 1, step 5 | medium | xrpl.js 5.2.0 | Name the option `signerXAddress` or document it inline; validate that Signers accounts are distinct with a message naming the option |
| 14 | protocol / expectation | Full repayment after the call date fails with `tecEXPIRED` | Once a scheduled payment is overdue, `LoanPay` needs `tfLoanLatePayment`, and that flag is mutually exclusive with `tfLoanFullPayment`, so an overdue loan cannot be closed in one transaction: the late coupon must be cleared first. Also, the call date is by construction the last due date, so "final repayment at the call date" is the last regular coupon, and `tfLoanFullPayment` only ever means an early close. Our first implementation sent a full payment after the call date. | LoanPay 0CAB4645F86B (demo close run 3) | medium | xrpl.js 5.2.0, rippled 3.4.0-rc1 | Docs: a "how to close a loan" section covering on-time, late and early cases; consider letting `tfLoanFullPayment` settle overdue amounts in one transaction |
| 15 | protocol / security gap | Multisig lacks on-chain time restrictions, forcing 2nd key to be 100% software-governed (Zero-Human access) | XRPL `SignerListSet` defines *who* can sign, but has no native temporal semantics (no `SignAfter` or `ValidAfter` per-signer rule). For an AT1 bond requiring a strict Call Date, the 2nd signature must be held by an autonomous software daemon where **zero human access is allowed**; otherwise any human keyholder could unilaterally break the bond maturity. | S1 Q1 & Enforcer implementation | high | Core XRPL Multisig / XLS-66 | **Proposed fix**: 1) Native `SignerCondition` in `SignerListSet` allowing time-based eligibility (e.g. `SignAfter: rippleEpoch`); 2) In the interim, run the software enforcer daemon inside a hardware-isolated Confidential Enclave / HSM (e.g. AWS Nitro Enclave) with zero human operator keys. |
| 16 | sdk / dead_end | WalletConnect cannot address a custom network | xrpl-connect's WalletConnect adapter identifies the ledger by a CAIP-2 id and only knows `xrpl:0/1/2` (mainnet/testnet/public devnet). This hackathon ledger is NetworkID 4001. Pairing asks for `xrpl:2`, WalletConnect v2 downgrades required namespaces to optional, the wallet approves only the chains it supports, and every sign request is then rejected client-side with `Failed to sign transaction. Missing or invalid. request() chainId: xrpl:2` before the wallet sees it. | `frontend/src/lib/xrplConnect.ts` (`signPrepared`), session namespaces vs `hackathonDevnet.walletConnectId` | high (blocks every wallet signature on the devnet) | xrpl-connect 0.8.2, @walletconnect/sign-client bundled | A CAIP id convention for custom networks (`xrpl:<NetworkID>`), and a documented `network` object with node URLs that adapters honour; pick the request `chainId` from the approved session namespaces instead of the configured one |
| 17 | sdk / expectation | `WalletConnectAdapter.sign()` returns the bare signature as `tx_blob` | `sign()` sends `xrpl_signTransaction` with `autofill: true` hardcoded and returns `{ tx_blob: tx_json.TxnSignature }`: the signature hex, not a serialized transaction, so `submit(tx_blob)` can never work. `signAndSubmit()` has the same shape. | `node_modules/xrpl-connect/index-*.mjs`, `requestSignTransaction` / `sign` | high | xrpl-connect 0.8.2 | Return `encode(tx_json)` as `tx_blob`; expose `autofill` as an option (an app that autofilled for a custom network, NetworkID included, must be able to say `autofill: false`). Workaround: call `adapter.client.request()` directly and `encode()` the returned `tx_json` |
| 18 | sdk / expectation | `reconnect()` never resumes a WalletConnect session | `WalletManager.autoConnect()` → `reconnect()` → `connect(walletId)` again; the WalletConnect adapter always runs `SignClient.connect()` and waits for a new approval, never reads `client.session.getAll()`. With no `onQRCode` callback at that point the proposal is invisible, the promise hangs, `walletManager.account` stays null. Any app-side "connected" cache then lies, which surfaced as `Connect this account's own wallet before activating multisig`. | `frontend/src/lib/wallet.tsx` restore path (removed), `autoConnect: false` | medium (re-scan after every reload) | xrpl-connect 0.8.2 | Restore an approved session from SignClient storage before proposing a new pairing; emit the pairing URI on reconnect so a QR can be shown |
| 19 | sdk / workaround | No wallet adapter can produce a multisig `Signers` entry or a `LoanSet` `CounterpartySignature` | Both need a raw private key (`Wallet.sign(tx, true)` / `signLoanSetByCounterparty`). Adapters only sign a plain single-signer transaction; the xrpl-connect WalletConnect adapter requests the `xrpl_signTransactionFor` method at pairing but never calls it. And even a wallet that implements `xrpl_signTransactionFor` cannot produce the `LoanSet` counterparty signature: `signLoanSetByCounterparty` signs with dedicated hash prefixes (`0x43505400` single, `0x43504d00` multi, `ripple-binary-codec/hash-prefixes.js`, introduced by `fixCleanup3_4_0`), not the ordinary multisign prefix `0x534d5400`. Consequence: the operator key of each 2-of-2 account is generated and held by the backend; the owner's wallet only signs the one-time `SignerListSet` + `AccountSet` that installs it. | `docs/borrower-lender-custody.md`, `src/chain/ops.ts` `getOperatorFor` | high (custody model) | xrpl-connect 0.8.2, xrpl.js 5.2.0 | An adapter-level `signForMultisig(tx)` (returns a `Signer` entry) and `signLoanSetAsCounterparty(blob)`; or a documented recipe per wallet |
| 20 | docs / wrong_model | LoanPay flag values and the meaning of `tfLoanFullPayment` | We published wrong values ourselves (late `0x10000`, full `0x40000`). xrpl.js: `tfLoanOverpayment 0x10000`, `tfLoanFullPayment 0x20000`, `tfLoanLatePayment 0x40000`. We also labelled the call-date settlement as a `tfLoanFullPayment` payoff; on-chain (`EA805215F3`) it is a late coupon, because the call date is the last due date and a full payment only ever means an early close. | `FEEDBACK_REPORT.md` history, tx `EA805215F309…` flags `0x40000` | medium | xrpl.js 5.2.0 | A flags table with hex values in the LoanPay reference, plus one paragraph: at the final due date you pay a regular/late coupon and the loan closes by itself; `tfLoanFullPayment` = early close with `CloseInterestRate` + `ClosePaymentFee` |
| 21 | docs / doc_gap | First-loss capital has no plain-language lifecycle | A team member asked what "top up first-loss capital" meant. The answer is spread over `LoanBrokerSet` (`CoverRateMinimum`, `CoverRateLiquidation`), the `LoanBroker` entry (`CoverAvailable`), `LoanBrokerCoverDeposit` and `LoanManage`: the pool absorbs losses before depositors, `LoanSet` is refused while `CoverAvailable < CoverRateMinimum × DebtTotal`, the broker refills it. | question in session, Broker Hub top-up button | low | n/a | One lifecycle paragraph with a worked 10 % example next to `LoanBrokerCoverDeposit` |
| 22 | protocol / workaround | No way to rotate a compromised vault owner / loan broker key | `VaultCreate` and `LoanBrokerSet` bind to the submitting `Account`. When our broker seed was exposed (pasted in a README), the only remedy was regenerating everything (`fund:setup`), which also loses demo state; a RegularKey/SignerList on the broker account is the mitigation, not a rotation. | README history, `scripts/fund-and-setup.ts` | medium | xrpl.js 5.2.0 | Document the recommended key-management pattern for vault owners / brokers (RegularKey + SignerList from day one), or a `VaultSet` / `LoanBrokerSet` owner-transfer |
| 23 | tooling / expectation | DevEx hook tags documentation greps with a `tx_type` | `grep`/`sed` over README and docs, and `npm run check` output naming the `Batch` amendment, produced `tool_result` rows with `tx_type: LoanSet / LoanPay / Batch` although nothing was built or submitted; aggregation could inflate attempt counts. | `.xrpl-devex/sent.jsonl`, session `c5372392…` | low | xrpl-devex-hook 2.4.0 | Only tag `tx_type` when a `result_code` or a submit call is present |
| 24 | tooling / doc_gap | "Max 3 pages" for a markdown deliverable | `FEEDBACK_REPORT.md` is required at the repo root as markdown; a page count has no meaning for it and no renderer was available offline to check. | hackathon brief | low | n/a | State a word budget, or say the organizers render at a fixed font size |
| 25 | tooling / workaround | Nothing stops a devnet seed from reaching a public repo | Our live broker and enforcer seeds sat in the README for a day; found by a manual `git grep` for seed patterns. The hook already redacts seeds in its own events. | README history (commit `7e7ae87`) | medium | xrpl-devex-hook 2.4.0 | A pre-commit seed guard in the starter kit, reusing the hook's redactor patterns |
| 26 | protocol / workaround | No native way to bind a vault or loan broker to one borrower | A bond vault must lend to its issuer only. Neither `VaultCreate` nor `LoanBrokerSet` has a field restricting the `LoanSet` `Counterparty`; the only per-vault marker is the free-form `Data` field, where we store the bid (issuer address included). The rule is enforced by our shim and by the enforcer before it counter-signs, not by the ledger. | `src/chain/ops.ts` `originate`, `src/chain/enforcer/index.ts` `decideCounterSign` | medium | xrpl.js 5.2.0 | `LoanBrokerSet.AllowedCounterparty` (or a list), or accept a Credential type the borrower must hold; failing that, document `Data` as the place for such bindings |
| 27 | protocol / workaround | Vault owner has no native lever over withdrawals, forcing a second off-chain multisig on the depositor side | Confirmed against the XLS-0065 spec text: `VaultWithdraw` "does not respect the permissioned domain rules," and any shareholder can withdraw at will, bounded only by `AssetsAvailable`; `AssetsMaximum` and `tfVaultPrivate` gate deposits only. To keep a depositor's principal locked until the call date (distinct from the borrower-side gate in #15), we had to run a second 2-of-2 multisig + Enforcer instance on lender accounts, whose `decideWithdraw()` policy only cosigns yield-share redemptions and refuses anything touching principal shares before loan settlement. | `src/chain/enforcer/index.ts` `decideWithdraw`, `src/chain/ops.ts` `withdraw` vs `prepareWithdraw` | high | xrpl.js 5.2.0 | Optional owner-set `WithdrawalCapPerPeriod` (`Amount` or bp share of `AssetsTotal`) + `WithdrawalPeriodSeconds` on `VaultSet`, enforced by the ledger at `VaultWithdraw` time; mirrors real-world fund/bond redemption gates and would remove this second off-chain trust assumption entirely |
| 28 | protocol / workaround | A bid's funding window has no on-chain effect | The order book gives each bid an `expiresAt`, but nothing on the ledger expires a vault that never filled: deposits stay accepted, no loan exists, depositors' funds just sit. We run a background scan in the shim (`scanStalledOriginations`, every 30 s) that originates a partially funded bond at its deadline for whatever was raised (above a 50 % floor) or leaves it for depositors to withdraw manually. | `src/chain/ops.ts` `originateStalledIfPastDeadline`, `src/chain/server.ts` interval | medium | xrpl.js 5.2.0 | A deposit deadline on `VaultCreate` / `VaultSet` after which `VaultDeposit` is refused (and, ideally, an owner-triggered refund), so a funding window is a ledger fact rather than an off-chain timer |
| 29 | protocol / doc_gap | Whitelisting is asymmetric: depositors can be gated natively, borrowers cannot | An AT1 issuance is institutional on both sides (KYC'd investors, a known issuer). XLS-65 gives the depositor side a native whitelist: `VaultCreate` with `tfVaultPrivate` and a `DomainID` restricts `VaultDeposit` to holders of the domain's Credentials (`PermissionedDomains` and `Credentials` are enabled on this devnet, verified by `npm run check`). Nothing equivalent exists for the borrower side: `LoanBrokerSet` has no `DomainID` and no counterparty list, so who may be the `Counterparty` of a `LoanSet` is entirely the broker's discretion (#26). And the deposit-side gate stops at deposits: the spec says `VaultWithdraw` "does not respect the permissioned domain rules" (#27), so an investor who loses accreditation keeps full liquidity. We did not build the depositor whitelist in the 36 h; it is the natural way to express the real-world minimum ticket / accredited-investor rule (§8 of our brief). | `VaultCreate` fields (`DomainID`, `tfVaultPrivate`), `LoanBrokerSet` fields (none), `src/chain/ops.ts` `createBond` | medium | xrpl.js 5.2.0 | `LoanBrokerSet.DomainID` (a borrower must hold a credential in the domain to be a `LoanSet` counterparty), symmetrical to the vault's; optionally a flag making a private vault's `VaultWithdraw` / share transfer respect the domain too; and a tutorial pairing `tfVaultPrivate` + Credentials for regulated issuances |
| 30 | tooling / expectation | Signing through a mobile wallet on a custom devnet is impractical for development and demos | Every signature is a phone round trip: scan the WalletConnect QR, approve in Xaman, wait for the relay. The session does not survive a page reload (#18), so each reload means a new scan. Activating the 2-of-2 is two consecutive approvals, and the first one expired while we approved the second (#17 window, `tefMAX_LEDGER`). Xaman cannot be pointed at this network, so it shows the transaction under a testnet/mainnet chain id, cannot display the account's real balance or history, and every test account has to be created by our script and its seed typed into the app by hand (`created_accounts.txt`). A full lifecycle exercised through the UI (deposit, 2/2 setup, yield withdrawal) is five or six phone interactions; a demo rehearsal takes ten minutes instead of one. Our teammate had written a local-seed adapter to avoid all this; we removed it to keep a single, real-wallet path. | `frontend/src/lib/xrplConnect.ts`, `ConnectWalletModal.tsx`, this session's rehearsal | medium | xrpl-connect 0.8.2, Xaman | A first-class *developer signer* in `xrpl-connect` (seed or keypair held in the tab, clearly labelled dev-only) so the real wallet is only needed for the final demo; custom-network support in Xaman (node URL + NetworkID); session resume; and a hackathon-provided devnet-aware wallet or browser extension pre-configured for the custom network |
| 31 | protocol / workaround | No native primitive for a conditional/escrowed lender offer, so a "locked" bid is only an off-chain convention | Rebuilding the order book so a lender's bid locks pending the borrower's accept/decline (`feature/bid-lock-accept-decline`) surfaced the same gap as #15 and #27 a third time: `VaultDeposit` is immediate and unconditional, there is no "reserve my funding, release on issuer approval" transaction type. Our bid stays a plain off-chain JSON record (`src/chain/trancheBookStore.ts`, write-serialized against races); an accepted lender can still simply never fund, and nothing detects or penalizes that. | `src/chain/trancheBookStore.ts` `acceptBid`/`declineBid`, `frontend/src/lib/chainClient.ts` `respondToBid`/`fundBid` | medium | xrpl.js 5.2.0 | `TokenEscrow` (XLS-85): lock the bid's XRP in escrow, released via `EscrowFinish` on the borrower's accept or returned via `EscrowCancel` on expiry/decline — a real on-chain lock instead of a label; not built (forces the lender to commit capital before acceptance, a real friction trade-off) |
| 32 | protocol / wrong_model | No interest-only (bullet) schedule: every LoanPay amortises principal | Our coupon modal let the issuer type a coupon amount (it suggested principal × rate / 2, e.g. 45 XRP) while the backend paid the ledger's `PeriodicPayment`, about 333 XRP for a 1,000 XRP loan over 3 instalments: an XLS-66 loan repays principal / PaymentTotal plus interest at every due date, and `LoanSet` has no field for an interest-only schedule with principal at maturity. A real AT1 pays interest-only coupons and principal at the call. The enforcer refuses any other amount and the ledger would too. Fixed the UI to show the true instalment split. | `frontend/src/components/CouponModal.tsx` (before/after), `LoanSet` fields | medium | xrpl.js 5.2.0 | A `PaymentType` (amortising / interest-only bullet) on `LoanSet`, or at least a spec note that `PeriodicPayment` always carries principal; and expose the principal / interest split of the next instalment in the `Loan` entry |
